language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-poojan' ]; then cd .. && eval "mv $currentfolder generator-poojan" && cd generator-poojan; fi

